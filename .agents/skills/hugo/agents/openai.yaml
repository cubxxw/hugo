interface:
  display_name: "Hugo"
  short_description: "Repo-specific patterns and workflows for hugo"
  default_prompt: "Use the hugo repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true